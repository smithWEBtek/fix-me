<h1>Dogs up for Adoption:</h1>

 <!-- move to partial -->

	<%= render partial: "dogs/dogs", locals: {dogs: @dogs, lonely_dogs: shelter_dogs} %>


	
	<%# binding.pry %>
 
<h1>Adopted Dogs:</h1>
<table>
  <tr>
    <th>Name</th>
    <th>Breed</th>
    <th>Age</th>
    <th>Sex</th>
    <th>Owner</th>
	</tr>
	
<% @dogs.each do |dog|%>
  <% if dog.owner %>
  <tr>
	 
		<td><strong><%= link_to dog.name, dog_path(dog)%></strong></td>

    <td><%= dog.breed%></td>
		<td><%= dog.age%></td>
		

		<td><%= gender(dog) %></td>
 
    <td><%= link_to dog.owner.name, owner_path(dog.owner) %></td>
    <td><button><a href="/dogs/<%=dog.id%>/abandon">Abandon Me!</a>
  </tr>
  </tr>
  <% end %>
<% end %>
</table>

<!-- use shelter_dogs helper method -->
<!-- move to partial -->
 

<!-- <h1>Shelter Dogs</h1> -->
 